# Docker Compose for running tests in isolation
services:
  # Redis - Message broker for Celery
  redis:
    image: redis:7-alpine
    container_name: mafqood_test_redis
    ports:
      - "6380:6379"
    environment:
      - REDIS_LOGLEVEL=warning
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - mafqood_test_network

  # Celery Worker for tests
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mafqood_test_worker
    command: celery -A app.celery_app worker --loglevel=info --concurrency=2 --time-limit=7200 --soft-time-limit=6900 -O fair --max-tasks-per-child=100 --without-gossip --without-heartbeat --without-mingle
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CHROMA_DB_PATH=/tmp/test_chroma_db
      - CV_CTX_ID=-1
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - .:/app
      - /tmp/test_chroma_db:/tmp/test_chroma_db
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - mafqood_test_network

  # Test runner service
  test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mafqood_test_runner
    command: pytest tests/test_workflow_integration.py -v --tb=short --color=yes
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CHROMA_DB_PATH=/tmp/test_chroma_db
      - CV_CTX_ID=-1
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONPATH=/app
      - RUN_E2E=1
      - INSIGHTFACE_OFFLINE=1
    volumes:
      - .:/app
      - /tmp/test_chroma_db:/tmp/test_chroma_db
    depends_on:
      redis:
        condition: service_healthy
      worker:
        condition: service_started
    networks:
      - mafqood_test_network

volumes:
  test_data:
    driver: local

networks:
  mafqood_test_network:
    driver: bridge
